NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Signs Spark workspace sessions. Must be set to a long random value before
# the invitation gate protects anything. Without it the code falls back to a
# development string that is not a secret.
SPARK_SESSION_SECRET=
