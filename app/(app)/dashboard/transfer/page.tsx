export default function TransferPage() {
  return (
    <main className="dashboard-page" aria-labelledby="transfer-title">
      <p className="eyebrow">Transfer</p>
      <h1 id="transfer-title">
        Transfer values and resources to future generations.
      </h1>
      <p>
        This empty shell will later support estate awareness, succession, values
        transfer, and family legacy topics.
      </p>
      <section className="empty-state-card" aria-label="Transfer placeholder">
        <h2>No Transfer items yet.</h2>
        <p>Legacy planning recommendations will be added after future data work.</p>
      </section>
    </main>
  );
}
